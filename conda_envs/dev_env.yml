name: ats-linter-dev
channels:
  - conda-forge
dependencies:
  - python >= 3.8
  - pytest
  - pytest-mock
  - pytest-cov
  - pytest-fixture-config
  - tox
variables:
    PIP_DISABLE_PIP_VERSION_CHECK: 1